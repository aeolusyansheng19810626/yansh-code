[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "yansh-code"
version = "0.1.0"
requires-python = ">=3.9"

[project.scripts]
yansh = "main:main"

[tool.setuptools]
py-modules = ["main", "agent", "tools", "config", "monitor", "interrupt"]
